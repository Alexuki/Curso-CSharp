@model string

@{
    var isRequired = ViewData.ModelMetadata.IsRequired;
    var style = isRequired ? "background-color: lightyellow" : "";
}

@Html.Password("", Model, new { @class = "form-control", placeholder = "Your password", style = style })